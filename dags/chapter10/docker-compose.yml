version: "3.7"
services:
  airflow:
    image: airflowbook/airflow:latest
    ports:
      - "8080:8080"
    networks:
      - airflow
    volumes:
      - ./dags:/root/airflow/dags
      - ./data:/data

networks:
  airflow:
